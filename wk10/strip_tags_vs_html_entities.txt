if we do not sanitize the input of the user, the user can send a script tag that can execute anything from the user input.

Strip Tags will avoid the execution of any script that the user tries to put in the user input

